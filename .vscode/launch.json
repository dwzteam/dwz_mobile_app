{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://localhost:2020",
            "webRoot": "${workspaceFolder}"
        }
    ]
}